# arch/meson.build

# 'target_arch' is passed down from the root meson.build.
# If target_arch is 'x86_64', it enters the 'x86_64' directory.

message('Entering architecture directory: ' + target_arch)

subdir(target_arch)
