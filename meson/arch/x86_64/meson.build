# arch/x86_64/meson.build

message('>>> Setting up x86_64 environment')

# 1. Definieer de SPECIFIEKE include map (waar aggregator.h staat)
# Dit pad is relatief aan dit bestand, dus het wijst naar 'arch/x86_64/include'
arch_inc = include_directories('include')

subdir('examples')
